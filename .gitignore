# Binaries
/routerllm
/routerlm
/api
*.exe
*.exe~
*.test
*.out
*.log

# Environment
.env
routerllm.yaml
cline-accounts.json
alysis-accounts.json

# Request-event telemetry (written beside the config at runtime)
routerllm-telemetry.jsonl
routerllm-telemetry.jsonl.tmp

# IDE / OS
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Frontend — internal/admin/dist is intentionally committed: go:embed needs it,
# so `go build ./...` must work in a clean clone without running Node.
web/node_modules/
web/.vite/

# Admin write-back backups
*.bak

# Self-signed TLS cert for the admin console (generated on first run)
admin-tls.crt
admin-tls.key

# Project memory
memory-bank/

# ZCode session metadata
.zcode/
